# Workflows

Each file here is a markdown SOP: objective, required inputs, which `tools/` script(s) to call, expected outputs, and edge-case handling. Add one per repeatable task (e.g., `build_notebook.md`, `generate_quiz.md`) as those tasks come up — don't pre-create workflows for tasks that don't exist yet.
