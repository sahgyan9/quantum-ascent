# Tools

Deterministic Python scripts that do the actual execution (notebook scaffolding, grading, data prep, API calls). Add scripts here as workflows require them — check this directory before writing a new one.
